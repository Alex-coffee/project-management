import { ProcessDirective } from './process.directive';

describe('ProcessDirective', () => {
  it('should create an instance', () => {
    const directive = new ProcessDirective();
    expect(directive).toBeTruthy();
  });
});
