import { D3ChartDirective } from './d3-chart.directive';

describe('D3ChartDirective', () => {
  it('should create an instance', () => {
    const directive = new D3ChartDirective();
    expect(directive).toBeTruthy();
  });
});
