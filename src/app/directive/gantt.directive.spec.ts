import { GanttDirective } from './gantt.directive';

describe('GanttDirective', () => {
  it('should create an instance', () => {
    const directive = new GanttDirective();
    expect(directive).toBeTruthy();
  });
});
